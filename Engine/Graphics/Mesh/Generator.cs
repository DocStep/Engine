namespace Engine;


public static class Generator {

    public static float chunkSize = 1;

    //public void HeightsGenerate (ChunkData data) {
    //    int res = chunkHeightmapResolution+1;
    //    for (int x = 0; x < res; x++)
    //        for (int y = 0; y < res; y++) {
    //            data.heightmap[x, y] = HeightGenerateCustom_task(PosInner_pos2(data.Pos, new Vector2(x, y)));
    //        }
    //}


    //public static Vector2 PosInner_pos2 (Vector2Int Pos, Vector2 posInner) {
    //    return chunkSize*new Vector2(Pos.x + posInner.x/(chunkHeightmapResolution),
    //        Pos.y + posInner.y/(chunkHeightmapResolution));
    //}

}
